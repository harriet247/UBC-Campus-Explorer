{
	"title": "Invalid for having empty AND",
	"query": {
		"WHERE": {
			"AND": {

			}

		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_avg",
				"courses_title",
				"courses_uuid"
			],
			"ORDER": "courses_avg"
		}
	},
	"isQueryValid": false,
	"result": "InsightError"
}
